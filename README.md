# SQL Problem Solutions

This repository contains a collection of my solutions to SQL problems, likely from platforms like LeetCode.

## Structure

The solutions are organized by difficulty within the `SQL` directory. Each difficulty level (e.g., `Easy`, `Medium`, `Hard`) has its own subdirectory.
